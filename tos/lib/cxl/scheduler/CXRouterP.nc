module CXRouterP {
} implementation {
  //- start off with search schedule
  //- once you get synched up to the base station, you should get a
  //  range of tier 0 (router + BS) and tier 1 (router + leaf) slot
  //  numbers.
  //- request a tier 0 slot for yourself (see CXLeafP)
  //- offer tier 1 slots for leaves (see CXBasestationP)
}
